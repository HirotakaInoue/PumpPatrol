# Pump Patrol Web

This frontend is scaffolded with Next.js 14, TypeScript, ESLint, and Prettier. Use pnpm for all
package management commands:

```bash
pnpm install
pnpm dev
pnpm lint
pnpm format
```
